# Repo-root conftest. Anchors pytest root discovery and ensures all packages
# are importable when pytest is invoked from the project root without installation.
# Runtime behavior is provided entirely by pyproject.toml [tool.pytest.ini_options].
